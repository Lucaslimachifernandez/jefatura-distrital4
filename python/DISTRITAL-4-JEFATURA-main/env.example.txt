# Variables de entorno para el proyecto Distrital 4 Jefatura
# Copia este archivo a .env y completa los valores

# Puerto del servidor
PORT=3001

# Entorno de ejecución (development, production)
NODE_ENV=development

# Configuración de la base de datos PostgreSQL
# En Render, DATABASE_URL se proporciona automáticamente
# Para desarrollo local, puedes usar estas variables:
DB_HOST=localhost
DB_PORT=5432
DB_NAME=distrital4
DB_USER=postgres
DB_PASSWORD=tu_contraseña_aqui

# O usa DATABASE_URL directamente (formato: postgresql://user:password@host:port/database)
# DATABASE_URL=postgresql://user:password@host:port/database

# JWT Configuration
JWT_SECRET=tu_secret_key_muy_segura_minimo_32_caracteres_aqui
JWT_EXPIRES_IN=8h

# Contraseña por defecto del usuario admin (cambiar en producción)
ADMIN_DEFAULT_PASSWORD=hijoteamo2

# CORS - Orígenes permitidos (separados por comas)
# En producción, especifica los dominios permitidos
ALLOWED_ORIGINS=*

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# Sincronización de base de datos (alterar tablas existentes)
# Solo usar en desarrollo. En producción debe ser false
DB_ALTER=false

# Frecuencia de backups (daily, weekly, monthly)
BACKUP_FREQUENCY=daily

